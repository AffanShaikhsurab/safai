# Safai landing

Marketing site for Safai — Nebula / Void pixel night-sky design language from the desktop app.

```bash
cd landing
npm install
npm run dev
```

Opens at http://localhost:5173. Use the **Nebula / Void** toggle in the nav (same sky, two palettes).

Build: `npm run build` → `landing/dist`.
