[package]
name = "safai-rules"
version = "0.1.0"
edition = "2021"
license = "MIT"
description = "Safai cleanup detection engine (rules, categories, safety tiers)"

[dependencies]
safai-core = { path = "../safai-core" }
serde = { workspace = true }
serde_json = { workspace = true }

[dev-dependencies]
tempfile = "3"
